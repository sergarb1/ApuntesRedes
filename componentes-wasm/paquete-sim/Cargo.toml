[package]
name = "paquete-sim"
version = "0.1.0"
edition = "2021"
description = "Simulador didáctico de envío de paquetes en una red — Apuntes PAR U01"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
